🔄 **Live page** — refreshed daily during the on-call week (2026-07-21 → 2026-07-28). Last refreshed **2026-07-23 08:03 AM PT (America/Los_Angeles)**. This page freezes at the Tuesday handoff (2026-07-28); a new page opens for the next week.

# Growth Team Ops Review — Weekly Handoff

**07/23/2026 Growth Team Ops Review** · On-call week **Tue 2026-07-21 00:00 → Tue 2026-07-28 00:00 (America/Los_Angeles)**, reported **week-to-date (window start → now, \~2.3 days / \~55 h in)**. Source: incident.io + Datadog, read-only (+ Jira for vulnerabilities). **Last refreshed: 2026-07-23 08:03 AM PT (America/Los_Angeles).**

_This on-call week — primary: **aiden.ramgoolam**; secondary: **Edder Núñez** (shift through Tue 2026-07-28 10:00 PT; verified live via_ `schedule_show`_). Next handoff Tue 2026-07-28: **Edder Núñez** primary, **shashank** secondary._

✅ **incident.io connector restored this run.** It was unavailable on the 2026-07-22 run (that refresh was Datadog + Jira only). This run re-read incident.io and reconciled the window: **0 paging alerts** and **0 incidents** for Jul 21 → now (confirmed via `alert_stats` / `incident_list`), and the Datadog monitor flaps carried on Jul 22 are confirmed to have created **no** incident.io alerts. On-call and the still-firing set were re-verified live.

## SLOs / SLAs (15 minutes)

* [Consolidated PENG-Growth Ops Dashboard (Datadog)](https://app.datadoghq.com/dashboard/eu4-i7d-r48/peng-growth-ops-dashboard)
* [PENG Bugs OOSLA (Jira)](https://earnin.atlassian.net/jira/dashboards/10779)
* [Vulnerabilities (Jira)](https://earnin.atlassian.net/issues/?filter=15295)

**Auto-generated alert-volume summary (week-to-date, \~2.3 days into the on-call week; incident.io + Datadog, read-only).** incident.io paging alerts: **0 total (0 High, 0 Low)** this week-to-date | Prior full week (Jul 14 → Jul 21): **5 (3 High, 2 Low)**, all resolved | Prior same-elapsed slice (first \~2.3 days of last week): **1 (High)** | **Trend: run-rate \~0/wk vs prior 5/wk → ↓** (quiet week so far; verdict from run-rate — week-to-date is partial). Human-attention: **0** | Auto-resolved: **0** (no incident.io alerts). Escalation rate (alerts → incidents): **0/0 (n/a — no paging alerts)**. **Still firing: 0 active / 3 stale (incident.io).** Datadog monitor-event firehose over the window: **174** state-change events across **5** monitors (first-mile-calc backlog 72, svc-mark-tech dev/stage latency 66, deactivated-user backlog 18, user-activation backlog 14, conversational-onboarding latency 4) — this is the raw trigger+warn+recovery stream, _not_ deduped pages; all are transient auto-recovering flaps and **none created an incident.io alert** (down from 321 firehose events the prior week). _Priority = monitor severity (High/Low); Warn/Alert = trigger level — a High-priority monitor can fire only at Warn._

## Incidents (15 minutes)

### Production Incidents — Customer Impact

**No production incidents this week** (Jul 21 → now). incident.io `incident_list` returned 0 incidents for the window, and Datadog shows 0 monitors in Alert/Warn — no customer-impact incident.

### Operational Incidents — Deploys / Data Repairs / Infra

**No operational incidents this week** (Jul 21 → now). 0 incidents of any type in-window (incident.io, confirmed live this run).

## incident.io Alerts / Monitoring (15 minutes)

### Required Human Attention — Acknowledged by oncall

**No alerts required human attention this week.** incident.io recorded **0 paging alerts** for the window (`alert_stats` = 0), so there were no acknowledgements or escalations to on-call. On-call this week: primary **aiden.ramgoolam**, secondary **Edder Núñez** (verified live via `schedule_show`).

### Auto-Resolved — Escalation Cancelled

_No incident.io alerts were created this week, so there were no escalations to cancel. For visibility, the Datadog monitor crossings below fired and self-recovered in-window **without creating an incident.io alert** (confirmed: 0 incident.io alerts for the window). On-call / ack: n/a — none paged._

* **TL;DR:** The first-mile-calc SQS queue (`first-mile-new-user-score`, monitor [137629294](https://app.datadoghq.com/monitors/137629294)) briefly crossed its backlog threshold 5 times across the window (incl once overnight, Jul 23 00:17 PT) and self-cleared within \~2–4 min each; no customer impact; created no incident.io alert.

    **What happened:** Observed — monitor 137629294 cycled Warn(>60s) → Alert(>90s) → Recovered repeatedly (72 Datadog events; 5 Alert crossings: Jul 21 06:18, Jul 22 05:00, Jul 22 18:21, Jul 22 21:20, Jul 23 00:17 PT). Query `avg(last_5m):avg:aws.sqs.approximate_age_of_oldest_message{name:first-mile-new-user-score} by {queuename} > 90` — no `env` scope, no sustain window; prod branch routes `@pagerduty-Activation-Alerts` + `@webhook-incidentio-high`. Currently OK (Datadog). incident.io recorded 0 alerts for the window, so this did not page. Likely cause: a transient SQS backlog that drains within one eval window (a consumer catch-up blip), not a sustained backlog; the routing branch `{{#is_match "env.name" "prod"}}` likely does not resolve on this un-`env`-scoped metric, so the paging handles are never included (consistent with 0 incident.io alerts across 5 Alert crossings this week and \~191 flaps last week). Cause beyond transient backlog not determined from available signals.


* **TL;DR:** The deactivated-user SQS queue (`deactivate-user`, monitor [137629364](https://app.datadoghq.com/monitors/137629364)) crossed its backlog threshold 5 times — three of them overnight (\~01:2x PT on Jul 21, 22 and 23) — each self-clearing in minutes; no customer impact; created no incident.io alert (nobody was paged overnight).

    **What happened:** Observed — monitor 137629364 cycled Warn(>60s) → Alert(>90s) → Recovered 5 times: Jul 21 01:23 PT, Jul 21 13:24 PT, Jul 22 01:23 PT, Jul 22 13:23 PT, Jul 23 01:24 PT (a near-clockwork \~01:2x + \~13:2x PT pattern; 18 Datadog events). Query `avg(last_5m):avg:aws.sqs.approximate_age_of_oldest_message{name:deactivate-user} by {queuename} > 90` — no `env` scope, no sustain; prod branch routes `@pagerduty-Activation-Alerts` + `@webhook-incidentio-high`. Currently OK. The three \~01:2x PT crossings would be overnight High pages _if_ the prod routing branch resolved — but incident.io recorded 0 alerts, confirming they did not page (same `env.name` non-resolution as the sibling above). Likely cause: transient overnight/afternoon queue backlog draining within an eval window. Cause beyond transient backlog not determined.


* **TL;DR:** The user-activation SQS queue (`prod_statusactivationattempt`, monitor [137629650](https://app.datadoghq.com/monitors/137629650)) crossed its 150s backlog threshold 3 times (all evening, \~21:2x PT) and self-cleared within minutes; no customer impact; created no incident.io alert.

    **What happened:** Observed — monitor 137629650 cycled Warn(>120s) → Alert(>150s) → Recovered 3 times: Jul 21 21:23 PT, Jul 22 21:21 PT, Jul 22 21:49 PT (14 Datadog events). Query `avg(last_5m):avg:aws.sqs.approximate_age_of_oldest_message{queuename:prod_statusactivationattempt} by {queuename} > 150` — prod-scoped by `queuename`, no sustain window; prod branch routes `@pagerduty-Activation-Alerts` + `@webhook-incidentio-high`. Currently OK. 0 incident.io alerts for the window (did not page). Likely cause: transient evening backlog on the activation-attempt queue, drained within minutes. Cause beyond transient backlog not determined.


* **TL;DR:** A new prod latency monitor on svc-conversational-onboarding (`trace.fastapi.request` p99 > 10s, monitor [259552001](https://app.datadoghq.com/monitors/259552001), P5) flapped twice on Jul 22 (\~09:04–09:14 PT), each self-recovering in \~2 min; Slack-only routing, no page, no customer impact.

    **What happened:** Observed — monitor 259552001 (P5, `env:prod`) triggered Jul 22 09:04 PT and 09:09 PT and recovered \~2 min after each, on `p99:trace.fastapi.request{env:prod,service:svc-conversational-onboarding} > 10` (`last_10m`). Routes to `@slack-growth-engineering-alerts` only (no incident.io / PagerDuty), so it never pages on-call — 0 incident.io alerts. New on the Growth stream this week; below the 3-fires/week bar but auto-resolved with no ack, so tracked in the ledger. Likely cause: a brief p99 latency spike (two samples over 10s) that cleared on its own — a slow-request blip, not sustained degradation. Cause beyond a transient latency spike not determined.



### Recurring / Flappy Alerts — Monitor Tuning Candidates

| Alert | Times Fired (this window, Datadog) | Notes |
| --- | --- | --- |
| [**301972958**](https://app.datadoghq.com/monitors/301972958) — svc-mark-tech "Request duration is too high" | \~66 events (12+ Alert / Recovered cycling) | **P5, non-prod (env:dev / dev-eks-cluster AND env:stage / microservices-cluster)** — routes to a non-prod channel. Largest single source of Datadog noise on the Growth stream (up from 16 last wk); 0 pages, 0 incident.io alerts. Tuning: confirm Growth ownership of `svc-mark-tech`; scope to `env:prod` or silence non-prod (ledger: svc-mark-tech baseline row). |
| [**137629294**](https://app.datadoghq.com/monitors/137629294) — first-mile-calc Processor Message Backlog | 72 events — Alert ×5 + Warn flaps | SQS `age_of_oldest_message > 90`, `last_5m`, no `env` scope, prod branch routes High. Self-clears \~2–4 min; 0 incident.io pages. Tuning: add `env:prod` + a sustain window; verify the prod routing branch actually resolves. Escalated (heavy flapping). |
| [**137629364**](https://app.datadoghq.com/monitors/137629364) — deactivated-user Processor Message Backlog | 18 events — Alert ×5 (3 overnight \~01:2x PT) + Warn flaps | SQS `age_of_oldest_message > 90`, `last_5m`, no `env` scope, prod branch routes High. Three consecutive nightly crossings but 0 incident.io pages. Tuning: add `env:prod` + a sustain window. |
| [**137629650**](https://app.datadoghq.com/monitors/137629650) — user-activation Processor Message Backlog | 14 events — Alert ×3 (evening \~21:2x PT) + Warn flaps | SQS `age_of_oldest_message > 150`, `last_5m`, prod-scoped by `queuename`, prod branch routes High. Same pattern; 0 incident.io pages. Tuning: add a sustain window. |
| [**259552001**](https://app.datadoghq.com/monitors/259552001) — svc-conversational-onboarding "Request duration is too high" **(new)** | 4 events — Alert ×2 (Jul 22 \~09:04–09:14 PT) | **P5, env:prod**, `trace.fastapi.request` p99 > 10s, routes `@slack-growth-engineering-alerts` only (no page). New this week; self-recovered \~2 min. Below the 3-fires/wk bar but auto-resolved with no ack → tracked. Tuning: watch for recurrence; if it recurs, add a sustain window (verify p99 baseline first). |

_None of the five created an incident.io alert this run (confirmed: 0 paging alerts for the window; and the backlog cluster historically does not page — last week's \~191 first-mile-calc Datadog flaps → 0 incident.io alerts). They are surfaced as tuning candidates, not pages that hit on-call._

### 🔧 Monitor Tuning Recommendations (learned)

Learned, **read-only** suggestions — **nothing here is auto-applied**; each row is a one-click `before → after` for a human to action in the monitor UI. **Feedback loop (2026-07-23, incident.io restored):** re-read the current Datadog config of all 11 diffable candidates (`135119948`, `133647342`, `143507582`, `17131362`, `137629294`, `137629364`, `137629650`, `27555488`, `243692163`, `243692043`, `143557417`) — all remain **unchanged** vs their recorded before-state, so none has been applied (**no validation win** this run). With incident.io back, the SQS backlog cluster is now confirmed to have paged **0** times despite 13 Alert crossings this week — strengthening the "verify the prod routing branch actually resolves" point. This week's top 5 by expected on-call impact are below; quiet items (0 fires: notif-preferences `243692163`/`243692043`, Apdex `27555488`, cashout-restore `143557417`, first-cashout anomaly `17131362`) live in the [Monitor Tuning Ledger](https://earnin.atlassian.net/wiki/spaces/~712020cb7ebe6a714e411e98574e2fb19d5faa/pages/5322604577).

| Monitor | Issue | Evidence (fires / weeks / auto-res) | Recommended change (before → after) | Confidence | Status |
| --- | --- | --- | --- | --- | --- |
| [**135119948**](https://app.datadoghq.com/monitors/135119948) — HPA sustained high utilization (job-user-setup-user-first-mile-calc-processor, env:prod) | Infra saturation autoscaling already handles — a High page that self-resolves in minutes with no incident; a Warn transition pages like a critical page. | 0 fires this wk; last fired Jul 16 (Warn 82.3%, acked by Alfred, self-resolved \~11 min, no incident); recurring \~4 wks; 0 incidents. | **Route HIGH → LOW (and/or gate the page to critical-only).** before: sustained-util >90 Alert / \~80 Warn → `@webhook-incidentio-high` + `@pagerduty-Activation-Alerts` (prod branch); a Warn transition pages the same as Alert. after: route the sustained-utilization branch → `@webhook-incidentio-low` (or scope the page handle to `is_alert`/critical only so Warn stops paging); keep OOM / pod-not-ready at HIGH; optional HIGH guard only if util >90% sustained >15 min. _Coverage preserved:_ a genuine capacity pin still pages HIGH. _Impact:_ removes \~2–3 interrupt-grade High pages/wk. | High — \~4-wk streak, human-acked last wk, 100% self-resolve, 0 incidents. | <custom data-type="status" data-id="id-0">strongly recommend</custom> |
| [**133647342**](https://app.datadoghq.com/monitors/133647342) — \[job-user-user-activation-processor\] sustained high memory utilization | Dev noise paging the prod High path + stale — a dev-eks-cluster group has been firing in incident.io since May 29 while prod reads OK. | Firing \~8 wks (since 2026-05-29), dev-eks-cluster group; Datadog OK on prod (re-verified today); 1 stuck High alert (01KST6RVJA…, confirmed still firing); 0 new fires. | **Scope out dev + clear the orphan.** before: mem-util ≥85% grouped by `kube_cluster_name`, `env:prod` — a dev-cluster group can still hold the prod High page open. after: exclude non-prod (add `cluster_flavor:prod` / `!kube_cluster_name:dev-eks-cluster`), route any dev branch to a dev Slack; then manually resolve the orphaned dev alert. _Coverage preserved:_ prod memory-utilization still pages High. _Impact:_ clears 1 stuck High and stops dev memory pressure paging prod on-call. | High — clear dev-scope leak; prod reads OK. | <custom data-type="status" data-id="id-1">strongly recommend</custom> |
| [**143507582**](https://app.datadoghq.com/monitors/143507582) — CM has requested more than one funnel cashout (job-cashout-user-cashout-status-processor) | Recurring REAL failure, not monitor noise — a duplicate funnel-cashout code/business-logic bug. The alert is also stuck firing (Datadog No Data). | Firing \~7 wks (since 2026-06-03); Datadog No Data (re-verified today); 1 stuck High alert (01KT752A…, confirmed still firing); 0 new fires. | **Do NOT tune — route to a Jira code fix.** before: monitor left as-is (it is catching a real condition). after: open/track a Jira fix for the duplicate funnel-cashout bug (runbook SRE/3082453072); once shipped, resolve or mute the stale alert. _Coverage preserved:_ monitor unchanged, so it keeps catching duplicates. _Impact:_ eliminates a recurring High and a months-old stuck alert. (Monitor SQL uses a `userid` placeholder — no customer value rendered.) | High — it is a bug, not noise (runbook + recurring since Jun 3). | <custom data-type="status" data-id="id-2">strongly recommend</custom> |
| [**137629294**](https://app.datadoghq.com/monitors/137629294) — Processor Message Backlog cluster (first-mile-calc `137629294`, deactivated-user [137629364](https://app.datadoghq.com/monitors/137629364), user-activation [137629650](https://app.datadoghq.com/monitors/137629650)) | Flappy SQS backlog with no sustain — three Activation-processor `age_of_oldest_message` monitors trip on transient <5-min backlog and auto-recover; all route High on the prod branch, but none actually pages. | This wk: first-mile-calc Alert ×5 (72 events), deactivated-user Alert ×5 incl 3 overnight (18), user-activation Alert ×3 (14); each self-clears \~2–11 min; \~2-wk recurrence; **0 incident.io pages confirmed** (13 Alert crossings → 0 alerts; last wk \~191 flaps → 0). | **Add a sustain window (+ env scope) and verify routing.** before: `avg(last_5m):…approximate_age_of_oldest_message… > 90` (150 for activation), no sustain; first-mile-calc + deactivated-user have **no** `env` scope; prod branch → `@pagerduty-Activation-Alerts` + `@webhook-incidentio-high` gated on `{{#is_match "env.name" "prod"}}` (which appears not to resolve). after: require the breach sustained ≥10–15 min (or ≥2 consecutive eval windows) so a 2–4 min drain stops flapping; add `env:prod` to the two unscoped queries; and **verify the prod routing branch actually pages** (it may be a latent coverage gap). _Coverage preserved:_ a sustained real backlog still pages High. _Impact:_ removes the bulk of this week's flap volume incl the overnight crossings. | Med — clear flappy-no-sustain pattern with live evidence this wk; confirmed 0 pages (routing likely not resolving). | <custom data-type="status" data-id="id-3">recommend</custom> |
| [**301972958**](https://app.datadoghq.com/monitors/301972958) — svc-mark-tech "Request duration is too high" (+ baseline siblings 301972959/960/961) | Dev/stage noise on the Growth stream — a P5 non-prod latency monitor flapping continuously; tagged `team:l2-peng-growth` but service is `svc-mark-tech` (likely not Growth-owned). | \~66 Datadog flap events this wk (env:dev / dev-eks-cluster + env:stage / microservices-cluster, P5); routes non-prod; 0 prod pages; 0 incident.io alerts. | **Ownership / scope review.** before: baseline-workflow monitor tagged `team:l2-peng-growth`, alerts on non-prod P5 latency, routes to a non-prod channel. after: confirm whether Growth owns `svc-mark-tech`; if not, re-tag to the owning team; if yes, scope to `env:prod` (or silence non-prod) so dev/stage flapping leaves the Growth stream. _Coverage preserved:_ prod latency (and OOM/not-ready on the 959/960/961 siblings) still alerts. _Impact:_ removes the single largest source of Datadog event noise on this team. | Med — high noise volume, low/no paging value; needs an ownership decision, not just a threshold. | <custom data-type="status" data-id="id-4">proposed</custom> |

_Also tracked in the_ [_ledger_](https://earnin.atlassian.net/wiki/spaces/~712020cb7ebe6a714e411e98574e2fb19d5faa/pages/5322604577)_: new this week — svc-conversational-onboarding p99 latency_ `259552001` _(P5 prod, Slack-only, 2 flaps → watch); and quiet (0 fires this week): the notif-preferences p90_ `243692163` _(gate the High page to_ `{{#is_alert}}` _/ route Warn→Low), the Apdex night-page_ `27555488` _(debounce_ `last_5m` _→_ `last_15m` _/ volume guard), the cashout-restore error-rate_ `143557417` _(add_ `env:prod` _+ min-volume guard), the notif-preferences avg-latency companion_ `243692043` _(Low; consider consolidating), the first-cashout anomaly_ `17131362` _(quiet last week incl the weekend → moves to resolved if quiet through Jul 28), and the Databricks Promotions-Metrics stale High (verify + clear). All recommendations are advisory — no monitor was changed by this agent._

### 🔴 Open Going Into Handoff

**Active prod Alert/Warn now: 0.** No Growth-owned monitor (prod or non-prod) is in Alert/Warn at refresh time (Datadog, live).

**Stale / lingering incident.io alerts: 3** (all High, all carryover from prior weeks; **confirmed still firing this run** via `alert_stats status:firing`; each alert's Datadog source reads No Data/OK, so these need a manual clear, not active prod work):

* **Monitor 143507582 — "CM has requested more than one funnel cashout"** (<custom data-type="status" data-id="id-5">High</custom>). incident.io alert 01KT752A4WBQQ6AKYNP8AB4AD6, firing since 2026-06-03. Datadog: <custom data-type="status" data-id="id-6">NO DATA</custom> (re-verified today). Duplicate-cashout code bug → Jira fix + resolve/mute (see tuning recommendation above).
* **Monitor 133647342 — "\[job-user-user-activation-processor\] sustained high memory utilization"** (<custom data-type="status" data-id="id-7">High</custom>). incident.io alert 01KST6RVJA4YYGHZS07M0N1T7V, firing since 2026-05-29 on `dev-eks-cluster` (non-prod). Datadog: <custom data-type="status" data-id="id-8">OK</custom> on prod (re-verified today). Orphaned dev-cluster fire → clear it; no prod action (see tuning recommendation above).
* **Databricks "Promotions Metrics Processor Job" — run failed** (workspace earnin-prod, <custom data-type="status" data-id="id-9">High</custom>). incident.io alert 01KCMWX2WHXP5F7R8KHHG3693E, firing since 2025-12-17 (>7 months). No live Datadog monitor. Verify the job has since succeeded → resolve.

**Handoff status:** **No active prod issues; 3 stale incident.io alert(s) to clear** (all carryover from prior weeks; confirmed still firing but their Datadog sources are quiet — No Data / OK). No active prod (or non-prod) monitor is in Alert/Warn at refresh time.

## Vulnerabilities, Velocity and Operational Costs (15 minutes)

**Vulnerabilities:** **16 open tickets** (org-wide scope; count as of 2026-07-23 08:03 AM PT via Jira filter 15295, `statusCategory != Done`, isLast:true) — **7 Critical, 9 High**. **Down from 21 (8 Critical / 13 High) on Jul 22** — 1 Critical + 4 High cleared (5 total). Criticals (7): **4** `golang.org/x/crypto` + 2 `websocket-driver` + 1 `tar`. High (9): SCA advisories — `axios` ×2, `undici` ×2, `picomatch`, `drizzle-orm`, `fast-uri`, `flatted` (8) — plus 1 SAST unsanitized-file-path finding (`_bmad/scripts/resolve_customization.py`; the optimizely-cleanup + list_customizable SAST findings cleared). No `svc-referral` / PENG-Growth-owned ticket is in today's set. [Vulnerabilities (Jira filter 15295)](https://earnin.atlassian.net/issues/?filter=15295). Scope is org-wide (projects incl. MOBPLAT / WEBPLAT / tooling repos), not Growth-specific; the Jira "priority" field reads Low on all — severity is the summary prefix (\[VM,SCA/SAST\] Critical/High). **Velocity:** TBD. **Operational Costs:** TBD.

## Velocity and Automation

TBD.

## Action Items

- [ ] **Review the 🔧 Monitor Tuning Recommendations (top 5) and the** [**Monitor Tuning Ledger**](https://earnin.atlassian.net/wiki/spaces/~712020cb7ebe6a714e411e98574e2fb19d5faa/pages/5322604577) — all read-only `before → after` suggestions; none applied yet. Highest impact first: (1) route HPA `135119948` High → Low; (2) scope `133647342` out of dev and clear the orphaned alert; (3) open a Jira code fix for the duplicate-cashout bug `143507582`. Apply in the monitor UI if you agree — the agent never changes monitors.
- [ ] **SQS backlog monitor cluster** (`137629294` first-mile-calc, `137629364` deactivated-user, `137629650` user-activation): add a sustain window (`last_5m` → ≥10–15 min sustained or ≥2 consecutive eval windows) and add `env:prod` to the two unscoped queries; keep High only for sustained backlog. Flapped 13 Alert crossings this week incl overnight (\~01:2x PT) — but created **0 incident.io alerts**, so **verify whether the prod routing branch (**`is_match env.name prod`**) actually pages**; if it never resolves, that is a latent coverage gap for a real sustained prod backlog.
- [ ] **svc-mark-tech dev/stage flapper** (`301972958` + baseline siblings `301972959`/`301972960`/`301972961`): \~66 non-prod latency flaps this week (P5, env:dev + env:stage) — the biggest noise source on the Growth stream. Confirm Growth actually owns `svc-mark-tech`; if not, re-tag to the owning team; if yes, scope to `env:prod` / silence non-prod.
- [ ] **NEW — svc-conversational-onboarding request-duration** (monitor `259552001`): P5 prod `p99:trace.fastapi.request > 10s`, Slack-only; 2 flaps Jul 22 (\~09:04–09:14 PT), self-recovered \~2 min. Watch for recurrence; if it recurs, add a sustain window (verify p99 baseline on the service first).
- [ ] **svc-notification-preferences p90 latency** (monitor `243692163`): gate the `@webhook-incidentio-high` handle to `{{#is_alert}}` (p90 > 1s) so a Warn (\~0.8s) crossing stops paging High; route the Warn tier to Slack/Low. Routing fix — no threshold change. (0 fires this week.)
- [ ] **cashout-attempt-restore error-rate** (monitor `143557417`): add `env:prod` + a minimum-volume guard (floor of SQS `hits` before the errors/hits ratio can alert) so a few errors at low volume don't trip 25%. (0 fires this week.)
- [ ] **svc-referral Apdex night page** (monitor `27555488`): consider a sustain window (`last_5m` → `last_15m` or ≥2 consecutive windows) and/or a minimum request-volume guard (verify overnight baseline first), or route transient dips to Low; keep High for sustained degradation. (0 fires this week.)
- [ ] **Key watch — first-cashout-volume anomaly** (monitor `17131362`): 0 fires again this week so far. If quiet through Jul 28 → mark resolved; a weekend re-fire reopens the question. Confirm on activation dashboard `kem-tug-987`.
- [ ] **Clear stale: duplicate funnel cashout** (monitor `143507582` / alert 01KT752A4WBQQ6AKYNP8AB4AD6): firing since Jun 3, Datadog No Data. Open/track a Jira code fix per the runbook, then resolve or mute.
- [ ] **Clear stale: dev-cluster memory alert** (monitor `133647342` / alert 01KST6RVJA4YYGHZS07M0N1T7V): firing since May 29 on dev-eks-cluster (non-prod); prod OK. Manually resolve — no prod action.
- [ ] **Clear stale: Databricks Promotions-Metrics alert** (01KCMWX2WHXP5F7R8KHHG3693E): firing since Dec 17 2025. Verify the job has since succeeded, then resolve.
- [ ] **Investigate the incident.io auto-resolution gap**: three High alerts remain firing weeks/months after their Datadog sources recovered. Re-check the webhook auto-resolve path and clear the backlog.
- [ ] **Vulnerabilities at 16 open org-wide (7 Critical, 9 High)**: prioritize the `golang.org/x/crypto` Critical cluster (4), the 2 `websocket-driver` Criticals, and the `tar` Critical; triage the remaining High SCA advisories (`axios`, `undici`, `picomatch`, `drizzle-orm`, `fast-uri`, `flatted`) and the 1 SAST file-path finding via filter 15295 / OOSLA dashboard. (Down 5 vs Jul 22.)

## 📝 Manual Notes (preserved across refreshes)

_Add notes here; they survive daily refreshes._

---

_Generated by the Growth Team Ops Review agent. Window: 2026-07-21 00:00 → 2026-07-28 00:00 America/Los_Angeles (week-to-date, \~2.3 days in). Last refreshed: 2026-07-23 08:03 AM PT. Sources: incident.io (read-only) + Datadog (read-only) + Jira (vulnerabilities). Customer identifiers redacted where present (monitor SQL uses a userid placeholder, not customer data). You can read #growth-engineering-alerts for more information. No monitoring configuration was changed by this agent._
